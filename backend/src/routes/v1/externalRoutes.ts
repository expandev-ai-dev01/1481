/**
 * @summary
 * External (public) API routes configuration for v1.
 * Handles public endpoints that don't require authentication.
 *
 * @module routes/v1/externalRoutes
 */

import { Router } from 'express';

const router = Router();

/**
 * @remarks Public routes will be added here
 * Example: router.use('/public', publicController);
 */

export default router;
