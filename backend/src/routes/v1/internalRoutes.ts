/**
 * @summary
 * Internal (authenticated) API routes configuration for v1.
 * Handles protected endpoints that require authentication.
 *
 * @module routes/v1/internalRoutes
 */

import { Router } from 'express';

const router = Router();

/**
 * @remarks Internal routes will be added here
 * Example: router.use('/appointments', appointmentRoutes);
 */

export default router;
